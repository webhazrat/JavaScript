// Selection Sort : 
// The selection sort algorithm sorts a array by repeatedly finding the minimum element from unsorted part and putting it at the begining

let unsortedArr = [11, 0, 8, 99, 25, 2, 1, 2];


for(let i=0; i < unsortedArr.length; i++){
    console.log(unsortedArr[i]);
    
}